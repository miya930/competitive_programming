#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    int ha, wa, hb, wb, hx, wx;
    cin >> ha >> wa;
    vector<string> a(ha);
    for (int i = 0; i < ha; i++) cin >> a[i];
    cin >> hb >> wb;
    vector<string> b(hb);
    for (int i = 0; i < hb; i++) cin >> b[i];
    cin >> hx >> wx;
    vector<string> x(hx);
    for (int i = 0; i < hx; i++) cin >> x[i];

    for (int i = 0; i < hx; i++) {
        
    }

    return 0;
}
