#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int calc(void) 
{
    if (vec.size() == n) {
        
    }
}

int main()
{
    int n;
    cin >> n;
    vector<vector<int>> a(2*n, vector<int>(2*n));

    for (int i = 0;  i < 2*n-1; ++i) {
        for (int j = i+1; j < 2*n) cin >> a[i][j];
    }

    
     

    return 0;
}
