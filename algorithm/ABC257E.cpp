#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    ll n, Q, x;
    cin >> n >> Q >> x;

    vector<ll> w(n), k(Q);   
    for (int i = 0; i < n; ++i) cin >> w[i];
    for (int i = 0; i < Q; ++i) cin >> k[Q];

    

    return 0;
}
