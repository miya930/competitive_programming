#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main()
{
    int n, h, r, t;
    cin >> n >> h >> r >> t;

    vector<int> ans(n);
    for (int i = 0; i < n; i++) {
        int ini = h + 2*r*(n-1);
        // f = h - 1/2*g*t^2
        
    }


    return 0;
}
